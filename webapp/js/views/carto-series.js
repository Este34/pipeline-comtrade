// Vue « Cartes & séries temporelles » — implémentation à venir.
export async function mount(container) {
  container.innerHTML = `<div class="empty">Cartes & séries — en cours de construction.</div>`;
}
