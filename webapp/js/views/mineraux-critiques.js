// Vue « Minéraux critiques » — implémentation à venir (dépend de l'extraction HS6).
export async function mount(container) {
  container.innerHTML = `<div class="empty">Minéraux critiques — en attente des données HS6 (extraction en cours).</div>`;
}
