// Vue « Analyse par produit » — implémentation à venir.
export async function mount(container) {
  container.innerHTML = `<div class="empty">Analyse par produit — en cours de construction.</div>`;
}
